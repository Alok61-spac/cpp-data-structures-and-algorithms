//steps for exicution
//1)save file
// open terminal and write g++ filename.cpp(for compile the code and convert into exicutable file)
///a.exe(to run exicutable file)

//boilerplate code
#include <iostream>
using namespace std;

int main(){     //it inform to system code starts from hear
    int number=8;
    cout<<number;

    return 0;    // it inform to system code ends hear
}